# 游戏帧数过高时对按键的检测会过于灵敏
# transform.scale不恰当使用会导致阴间bug出现
# 由于刷新速度和玩家按键速度之间的差异产生的画面延迟或画面连续该如何有效解决？
# 循环队列真讨厌，为了把循环队列写进去麻烦了不少，略略略（但是看起来帅一点）

# 使用memory.txt存放信息是暂时的，此部分或许需要一个更加适合的存档方式，有待实现
# 在init中调用函数和在update方法中调用函数导致的结果可能会有天壤之别
# 不要循环import

# 如何让敌人不能穿过障碍又能追逐玩家

# 会不会在第一关死了 继续游戏时血不满